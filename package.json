{
  "name": "cx360-dashboard",
  "version": "1.0.0",
  "scripts": {
    "postinstall": "npm --prefix backend install && npm --prefix frontend install",
    "build": "npm --prefix frontend run build",
    "prestart": "npm run build",
    "start": "npm --prefix backend start"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.80.0",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.3"
  }
}
